version: '3.1'

services:
  mongo:
    image: mongo:latest
    container_name: maven_mongodb
    ports:
      - "27017:27017"
    volumes:
      - ./maven_deps.bson:/data/maven_deps.bson
      - ./maven.bson:/data/maven.bson
